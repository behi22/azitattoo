declare var BUILD_ENV: 'production' | 'development';
declare var BUILD_VER: {
  name: string;
  release: string;
  branch: string;
  hash: string;
  timestamp: string;
  author: string;
};
declare var BUILD_BRANCH: 'DEV' | 'MASTER' | 'ST' | 'PRO';
